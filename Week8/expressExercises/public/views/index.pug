html
  head
    title= title
    link( type='text/css' rel='stylesheet' href='/static/css/style.css' )
  body
    div(class=wrapper)
        div(class=content)
            h1= welcome
            h2= message
            if animals
                ol
                    each animal in animals
                        if animal.favorite
                            li= animal.name